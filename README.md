# explorepages
Use sitemap.xml to list pages and then return the title and description length


Run pip install flask requests beautifulsoup4

Then run python app.py

open page in browser enter URL for sitemap

